overlay: 1.1.0
info:
  title: "Set apify.com main info.version so the spec has a resolvable API identifier"
  version: "1.0.0"
actions:
  - target: "$.info"
    description: "Apify's upstream spec ships an empty info.version, which leaves the API identifier (vendor/name/version) unresolvable on import. Set it to the API's documented major version (v2)."
    update:
      version: "2.0.0"
